name: in_memory_relations CI tests

on:
  push:
  pull_request:

jobs:
  build-nix:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: cachix/install-nix-action@v18
    - run: nix flake check --print-build-logs
    - run: nix build --print-build-logs
